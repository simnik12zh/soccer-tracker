# Anthropic API key for the AI coach serverless function (api/coach.js).
# Server-side only — do NOT prefix with VITE_, or it will be bundled into the
# public client JS and leak. Set the same key in your Vercel project env vars.
ANTHROPIC_API_KEY=sk-ant-...
